/**
 * dans un browser l'object global est "window"
 * dans node l'object global est "global"
 * 
 * dans le browser nous avons "document" qui permet de naviguer dans le DOM
 * dans nodejs nous avons "process" qui donne les info sur tout les process qui tournent dans node
 * 
 *  node est un event non blocking language , non blocking I/O model (un user peut demander des info sans bloquer les autres users d'effectuer d'autre operation
 *  
 *  npm (node package ecosystem) est utilisé dans node: cela sert à faire des packages
 *  www.npmjs.com
 */
global;
process;
process.exit(0); //on sort de nodejs
//ctrl d : on sort aussi de nodejs
node v; //version de node
